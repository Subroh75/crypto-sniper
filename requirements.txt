streamlit>=1.32.0
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0
fpdf2>=2.7.0
# Kronos-mini (optional AI forecast toggle)
# Requires torch and the kronos-ts package. Only installed if Kronos toggle is used.
# torch>=2.0.0   # ~500MB — not pre-installed on Streamlit Cloud free tier
# kronos-ts      # pip install kronos-ts (installs Kronos + KronosTokenizer + KronosPredictor)
